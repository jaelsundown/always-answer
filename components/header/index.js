import React from "react"

import Navbar from "./navbar"

const Header = () => (
  <header style={{ display: 'flex', flexDirection: 'row', zIndex: 'auto' }}>
    <Navbar />
  </header>
)

export default Header
